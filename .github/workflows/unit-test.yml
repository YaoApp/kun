name: UnitTest

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  unit-test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        go: [1.18]
    steps:
      - name: Setup Cache
        uses: actions/cache@v2
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
          key: ${{ runner.os }}-go-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-go-

      - name: Checkout Code
        uses: actions/checkout@v2

      - name: Setup Go ${{ matrix.go }}
        uses: actions/setup-go@v2
        with:
          go-version: ${{ matrix.go }}

      - name: Setup Go Tools
        run: |
          go mod download
          PATH="${GOPATH}/bin:${GOROOT}/bin:${PATH}"
          get -u github.com/client9/misspell/cmd/misspell
          echo $PATH 
          ls -l "${GOPATH}"
          ls -l "${GOPATH}/*"

      #     make tools
      - name: Run Test
        run: |
          make vet
          make fmt-check
          make misspell-check
          make test

      - name: Codecov Report
        uses: codecov/codecov-action@v2
